@model HOA.Model.ViewModel.ResubmitViewModel

@{
    ViewBag.Title = "New submission";
}


@using (Html.BeginForm("Submission", "Resubmit", FormMethod.Post, new { @class = "form-horizontal", enctype = "multipart/form-data" }))
{

    <h3>Resubmit</h3>

    @Html.AntiForgeryToken()

    @if (!Html.ViewData.ModelState.IsValid)
    {
        <div class="alert alert-danger" role="alert">
            <strong>Oh snap!</strong> Please fix the listed errors and try submitting again.

            @Html.ValidationSummary()
        </div>
    }

    @Html.HiddenFor(model => model.SubmissionId)

    <div class="form-group">
        <label class="control-label col-xs-3">
            @Html.LabelFor(model => model.Description)
        </label>
        <div class="col-xs-9">
            @Html.TextAreaFor(model => model.Description, new { @class = "form-control" })
        </div>
    </div>
    <div class="form-group">
        <label class="control-label col-xs-3">
            @Html.LabelFor(model => model.Files)
        </label>
        <div class="col-xs-9">
            <input type="file" name="Files" id="Files" multiple />
        </div>
    </div>
    <div class="form-group">
        <div class="col-xs-offset-3 col-xs-9">
            <input type="submit" value="Submit" class="btn btn-primary" />
        </div>
    </div>

}